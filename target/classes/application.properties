# Default 8081. Override with SERVER_PORT for a different port.
server.port=${SERVER_PORT:8081}
spring.application.name=traffic-management
# Optional. If unset, shortUrl uses http://localhost:<actual server port> after startup.
# Set explicitly behind a reverse proxy, e.g. https://links.example.com
# app.public-base-url=
